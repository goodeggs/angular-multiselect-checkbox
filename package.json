{
  "name": "angular-multiselect-checkbox",
  "version": "1.0.4",
  "description": "multiselect checkbox built for smart table",
  "author": "Good Eggs <open-source@goodeggs.com>",
  "contributors": [
    "dannynelson <danny@goodeggs.com>"
  ],
  "license": "MIT",
  "keywords": [
    "smart-table multiselect checkbox"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/goodeggs/angular-multiselect-checkbox.git"
  },
  "homepage": "https://github.com/goodeggs/angular-multiselect-checkbox",
  "bugs": "https://github.com/goodeggs/angular-multiselect-checkbox/issues",
  "dependencies": {
    "angular-indeterminate-checkbox": "^0.1.0"
  },
  "devDependencies": {
    "coffee-script": "^1.12.1"
  },
  "scripts": {
    "build": "rm -rf build/ && cp -R src build && npm run build:safe",
    "build:safe": "coffee --bare --compile build/ && cp package.json build/"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "always-auth": true
  }
}
